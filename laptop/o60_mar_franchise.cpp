#include <bits/stdc++.h>
using namespace std;
const int X = 1e3 + 5;
int N, K, M, v[X];
int main(){
	ios::sync_with_stdio(0), cin.tie(0);
	cin >> N >> K >> M;
	for(int i = 0; i < N; ++i) cin >> v[i];
	assert(K <= 30);
}
