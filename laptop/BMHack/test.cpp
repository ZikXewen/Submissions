#include <bits/stdc++.h>
using namespace std;
int main(){
    int N;
    ios::sync_with_stdio(0), cin.tie(0);
    vector<pair<int, int>> v;
    v.emplace_back(0, 1);
    for(auto [u, w]: v);
    cin >> N; cout << N;
}