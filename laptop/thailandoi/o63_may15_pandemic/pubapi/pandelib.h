#include <vector>

typedef std::vector<int> assignment_t;

void pandemic_init(int& N, int& K, int& L);
void pandemic_assign(assignment_t assignments[], bool results[]);
void pandemic_answer(int ans);

